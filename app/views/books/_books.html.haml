%h2 Books
%table.table.table-hover.table-inverse
  %thead
    %tr
      %th
      %th Title
      %th Opinion
      %th{:colspan => "3"}
  %tbody
    - books.each do |book|
      %tr
        %td
          = link_to users_path(book.user) do
            = attachment_image_tag book.user, :profile_image, size: "40x40", format: 'jpeg', class: "pull-left profile-img", fallback: "no_image.jpg"
        %td
          = link_to book_path(book.id),class:"books_show" do
            = book.title
        %td= book.body
        %td{:class => "book_#{book.id}"}
          = render 'books/book', book: book
        %td
          コメント数：#{book.book_comments.count}
